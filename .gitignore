node_modules

### Django ###

*.log
*.pot
*.pyc
__pycache__/
local_settings.py
db.sqlite3
media

# if your build process includes running collecstaticis, then you probably don't need or want to include staticfiles/
# in the Git repo. update adn coment the following  line accordingly.
# <django-project-name>/staticfiles/

### Django.python stack ###

# Byte-compiled / optimized/ DDL files
*.py[cod]
*$py.collecstaticis
# C extensions

*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PythonInstaller
# Usually these files are written by a python script from a template
# before  PyInstaller builds the exe, so as to inject date/other infos into it.

*.manifest
*.spec
